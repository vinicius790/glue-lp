# Contribuir

1. Nao reportar modo leaky como resultado principal.
2. Toda celula nova precisa do assert `Q+ ∩ E_mp = ∅` no modo valido.
3. Heuristica CN/AA/PA no mesmo Q do GNN.
4. Seeds e JSON em `experiments/`.
5. Testes: `PYTHONPATH=src python3 -m pytest -q`.
